<!DOCTYPE html>
<html>
<head>
  <title><%= @title || "Erreur" %> - Avo Admin</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      background: #F9FAFB;
      color: #111827;
      line-height: 1.5;
    }

    .header {
      background: white;
      border-bottom: 1px solid #E5E7EB;
      padding: 1rem 2rem;
    }

    .header h1 {
      font-size: 1.25rem;
      font-weight: 600;
      color: #374151;
    }

    .container {
      min-height: calc(100vh - 65px);
      display: flex;
      align-items: center;
      justify-content: center;
    }
  </style>
</head>
<body>
  <!-- Header simple -->
  <div class="header">
    <h1>Avo Admin</h1>
  </div>

  <!-- Contenu -->
  <div class="container">
    <%= yield %>
  </div>
</body>
</html>
