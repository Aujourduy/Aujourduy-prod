# Connexions locales
local   all             all                                     trust

# Connexions via Tailscale (tout le réseau Tailscale)
host    all             all             100.95.0.0/16           md5

# Connexions depuis les conteneurs Docker Compose (réseau interne)
host    all             all             172.18.0.0/16           trust
host    all             all             172.19.0.0/16           trust
host    all             all             172.20.0.0/16           trust

# (optionnel) Loopback interne
host    all             all             127.0.0.1/32            trust